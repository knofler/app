/*
 * MediaLive Messages
 *
 * This contains all the text for the MediaLive container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MediaLive';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MediaLive container!',
  },
});
