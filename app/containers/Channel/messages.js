/*
 * Channel Messages
 *
 * This contains all the text for the Channel container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.Channel";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Channel container!"
  }
});
