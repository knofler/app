/*
 * Genapp Messages
 *
 * This contains all the text for the Genapp container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Genapp';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Genapp container!',
  },
});
