/*
 * Location Messages
 *
 * This contains all the text for the Location component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.Location";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Location component!"
  }
});
