/*
 * Dislike Messages
 *
 * This contains all the text for the Dislike component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.Dislike";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Dislike component!"
  }
});
