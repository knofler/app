/*
 * ProcessCart Messages
 *
 * This contains all the text for the ProcessCart component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.ProcessCart";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ProcessCart component!"
  }
});
