/*
 * Channel Messages
 *
 * This contains all the text for the Channel component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ChannelCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Channel component!',
  },
});
