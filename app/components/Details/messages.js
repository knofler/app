/*
 * Details Messages
 *
 * This contains all the text for the Details component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.Details";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Details component!"
  }
});
