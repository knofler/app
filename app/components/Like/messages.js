/*
 * Like Messages
 *
 * This contains all the text for the Like component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.Like";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Like component!"
  }
});
