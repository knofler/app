/*
 * Scan Messages
 *
 * This contains all the text for the Scan component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.Scan";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Scan component!"
  }
});
