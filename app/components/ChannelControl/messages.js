/*
 * ChannelControl Messages
 *
 * This contains all the text for the ChannelControl component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ChannelControl';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ChannelControl component!',
  },
});
