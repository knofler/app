/*
 * CrudControl Messages
 *
 * This contains all the text for the CrudControl component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.CrudControl";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the CrudControl component!"
  }
});
