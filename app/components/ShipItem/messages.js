/*
 * ShipItem Messages
 *
 * This contains all the text for the ShipItem component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.ShipItem";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ShipItem component!"
  }
});
